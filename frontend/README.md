# Frontend (Vite + React)

This folder contains the Birthday Reminder UI.

For installation, environment variables, and how to run the full stack, see the **[root README](../README.md)**.

```bash
npm install
npm run dev
```

Configure `VITE_API_URL` in `.env` (copy from `.env.example`) to point at your backend API.
